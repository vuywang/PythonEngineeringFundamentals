# 1. 日志是什么？🤔

日志就是程序运行时留下的“记录”。

比如你写一个登录程序，日志里可以记录：

```text
2026-05-07 10:20:01 INFO 用户开始登录 username=admin
2026-05-07 10:20:02 WARNING 用户密码错误 username=admin
2026-05-07 10:20:05 ERROR 数据库连接失败
```

它的作用是：

* 记录程序运行流程 🧭
* 排查 bug 🐞
* 定位线上问题 🔍
* 记录异常堆栈 💥
* 分析系统行为 📊

---

# 2. 为什么不用 `print()`？🧐

`print()` 适合临时学习和简单调试，但真实项目里不够用。

比如：

```python
print("程序开始")
print("查询数据库")
print("出错了")
```

问题是：

* 没有时间
* 没有日志级别
* 不方便输出到文件
* 不方便开关
* 不方便区分模块
* 不适合长期保留

日志就不一样了。你可以控制哪些级别输出、输出到哪里、格式长什么样。Python 官方 Logging Cookbook 也明确提到，相比大量 `print` 调试，`logger.debug()` 可以长期保留在代码里，需要时只调整 logger 或 handler 的级别即可重新启用。([Python documentation][2])

---

# 3. 最简单的日志例子 🌟

```python
import logging

logging.warning("这是一个警告")
logging.info("这是一条普通信息")
```

你可能只会看到：

```text
WARNING:root:这是一个警告
```

为什么 `info` 没显示？

因为 Python logging 默认级别是 `WARNING`，也就是说默认只处理 `WARNING` 及以上级别的日志，`INFO` 和 `DEBUG` 默认不会显示。([Python documentation][1])

---

# 4. 日志级别是什么？📶

日志级别就是“这条日志有多严重”。

常见级别从低到高是：

| 级别         | 含义   | 什么时候用          |
| ---------- | ---- | -------------- |
| `DEBUG`    | 调试信息 | 变量值、流程细节、排查问题  |
| `INFO`     | 普通信息 | 程序启动、任务完成、用户操作 |
| `WARNING`  | 警告   | 有风险，但程序还能继续    |
| `ERROR`    | 错误   | 某个功能失败了        |
| `CRITICAL` | 严重错误 | 程序可能无法继续运行     |

比如：

```python
import logging

logging.debug("调试信息")
logging.info("普通信息")
logging.warning("警告信息")
logging.error("错误信息")
logging.critical("严重错误")
```

默认情况下，你一般只能看到 `WARNING`、`ERROR`、`CRITICAL`，因为默认门槛就是 `WARNING`。([Python documentation][1])

---

# 5. `basicConfig()`：快速配置日志 ⚙️

想让 `DEBUG`、`INFO` 也显示，可以这样：

```python
import logging

logging.basicConfig(level=logging.DEBUG)

logging.debug("debug 信息")
logging.info("info 信息")
logging.warning("warning 信息")
logging.error("error 信息")
logging.critical("critical 信息")
```

输出类似：

```text
DEBUG:root:debug 信息
INFO:root:info 信息
WARNING:root:warning 信息
ERROR:root:error 信息
CRITICAL:root:critical 信息
```

这里：

```python
level=logging.DEBUG
```

表示日志门槛设置为 `DEBUG`。

也就是说：

> `DEBUG` 及以上级别都会输出。

官方示例里也说明，当 `basicConfig(level=logging.DEBUG)` 设置门槛为 `DEBUG` 时，`DEBUG`、`INFO`、`WARNING` 等都会被打印。([Python documentation][1])

---

# 6. 日志级别门槛怎么理解？🚪

你可以把日志级别想成一个门卫。

如果配置：

```python
logging.basicConfig(level=logging.INFO)
```

意思是：

> `INFO` 及以上可以通过，`DEBUG` 不让过。

所以：

```python
import logging

logging.basicConfig(level=logging.INFO)

logging.debug("调试信息")
logging.info("普通信息")
logging.warning("警告信息")
```

结果：

```text
INFO:root:普通信息
WARNING:root:警告信息
```

`DEBUG` 没输出，因为级别太低。

---

# 7. 格式化日志输出 🧾

默认格式有点简单。我们可以自定义格式。

```python
import logging

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s %(levelname)s %(message)s"
)

logging.info("程序启动")
logging.warning("磁盘空间不足")
```

输出类似：

```text
2026-05-07 10:20:01,123 INFO 程序启动
2026-05-07 10:20:02,456 WARNING 磁盘空间不足
```

常见格式字段：

| 字段              | 含义        |
| --------------- | --------- |
| `%(asctime)s`   | 日志时间      |
| `%(levelname)s` | 日志级别      |
| `%(name)s`      | logger 名称 |
| `%(message)s`   | 日志内容      |
| `%(filename)s`  | 文件名       |
| `%(lineno)d`    | 行号        |
| `%(funcName)s`  | 函数名       |

官方教程也展示了通过 `format` 参数改变日志显示格式，例如 `%(levelname)s:%(message)s`，并说明简单场景常用 levelname、message、时间等字段。([Python documentation][1])

---

# 8. 推荐的基础格式 ✅

你平时可以先用这个：

```python
import logging

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(name)s:%(lineno)d - %(message)s",
    datefmt="%Y-%m-%d %H:%M:%S"
)

logging.info("程序启动")
```

输出类似：

```text
2026-05-07 10:20:01 [INFO] root:9 - 程序启动
```

这里：

```text
root:9
```

表示 logger 名称是 `root`，代码在第 9 行。

---

# 9. 日志输出到文件 📂

日志不一定只打印到控制台，也可以写入文件。

```python
import logging

logging.basicConfig(
    filename="app.log",
    encoding="utf-8",
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(message)s"
)

logging.info("程序启动")
logging.warning("这是警告")
logging.error("发生错误")
```

运行后，会生成：

```text
app.log
```

里面类似：

```text
2026-05-07 10:20:01,123 [INFO] 程序启动
2026-05-07 10:20:02,456 [WARNING] 这是警告
2026-05-07 10:20:03,789 [ERROR] 发生错误
```

官方教程里也展示了 `basicConfig(filename='example.log', encoding='utf-8', level=logging.DEBUG)` 这种写入文件的写法；从 Python 3.9 开始，`basicConfig` 支持 `encoding` 参数。([Python documentation][1])

---

# 10. `filemode="w"` 和默认追加模式 ⚠️

默认情况下，日志文件会追加写入。

也就是说你运行多次程序，日志会不断加在文件后面。

如果你想每次运行都重新覆盖日志文件，可以写：

```python
import logging

logging.basicConfig(
    filename="app.log",
    filemode="w",
    encoding="utf-8",
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(message)s"
)

logging.info("这次会覆盖旧日志")
```

官方教程说明，如果多次运行脚本，日志默认会追加到文件；如果想每次重新开始，可以给 `basicConfig` 传 `filemode='w'`。([Python documentation][1])

---

# 11. `logging` 模块级写法 vs `logger` 写法 🧩

前面这种是模块级写法：

```python
logging.info("程序启动")
```

它用的是 root logger。

但正式一点的项目，更推荐这样：

```python
import logging

logger = logging.getLogger(__name__)

logger.info("程序启动")
```

为什么？

因为：

```python
__name__
```

表示当前模块名。

比如你有文件：

```text
user_service.py
```

里面：

```python
logger = logging.getLogger(__name__)
```

那么 logger 名字通常就是：

```text
user_service
```

这样你看日志时，就知道是哪一个模块打出来的。官方文档也建议大多数程序更应该创建模块级 logger 并调用它的方法，而不是一直用 root logger 的模块级函数。([Python documentation][3])

---

# 12. 正式项目推荐模板 ⭐

新建一个 `main.py`：

```python
import logging

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(name)s:%(lineno)d - %(message)s",
    datefmt="%Y-%m-%d %H:%M:%S"
)

logger = logging.getLogger(__name__)


def login(username: str, password: str) -> bool:
    logger.info("用户开始登录 username=%s", username)

    if username != "admin":
        logger.warning("用户名不存在 username=%s", username)
        return False

    if password != "123456":
        logger.warning("密码错误 username=%s", username)
        return False

    logger.info("用户登录成功 username=%s", username)
    return True


if __name__ == "__main__":
    login("admin", "123456")
    login("test", "111111")
```

注意这里用了：

```python
logger.info("用户开始登录 username=%s", username)
```

而不是：

```python
logger.info(f"用户开始登录 username={username}")
```

这不是说 f-string 不能用，而是 logging 官方教程里推荐把格式字符串和变量参数分开传，logging 会使用 `%` 风格把变量合进去；这种写法也有性能和日志延迟格式化方面的好处。([Python documentation][1])

---

# 13. `logger.debug/info/warning/error/critical` 怎么用？🎚️

你可以这样判断：

## `debug`：给开发看的细节 🔍

```python
logger.debug("查询参数 user_id=%s, page=%s", user_id, page)
```

适合记录很细的调试信息。

---

## `info`：正常流程信息 ✅

```python
logger.info("用户登录成功 user_id=%s", user_id)
```

适合记录关键业务流程。

---

## `warning`：异常苗头，但还能继续 ⚠️

```python
logger.warning("库存不足 product_id=%s stock=%s", product_id, stock)
```

程序还能跑，但值得注意。

---

## `error`：操作失败 ❌

```python
logger.error("订单创建失败 user_id=%s", user_id)
```

某个功能失败了，但程序未必整体崩。

---

## `critical`：严重错误 💥

```python
logger.critical("数据库不可用，系统无法继续运行")
```

一般是非常严重的问题。

---

# 14. 记录异常：`logger.exception()` 💥

你刚学过异常，日志和异常经常一起用。

错误写法：

```python
try:
    result = 10 / 0
except ZeroDivisionError as e:
    logger.error("计算失败：%s", e)
```

这能记录错误信息，但没有完整堆栈。

更推荐：

```python
try:
    result = 10 / 0
except ZeroDivisionError:
    logger.exception("计算失败")
```

输出会带异常堆栈，排查问题更方便。官方文档说明，`logger.exception()` 会以 `ERROR` 级别记录消息，并把异常信息添加到日志中，而且应该只在异常处理器里调用。([Python documentation][3])

---

# 15. `logger.exception()` 实战例子 🧪

```python
import logging

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(name)s:%(lineno)d - %(message)s"
)

logger = logging.getLogger(__name__)


def divide(a: int, b: int) -> float:
    return a / b


try:
    divide(10, 0)
except ZeroDivisionError:
    logger.exception("除法计算失败")
```

输出类似：

```text
2026-05-07 10:20:01,123 [ERROR] __main__:18 - 除法计算失败
Traceback (most recent call last):
  File "main.py", line 16, in <module>
    divide(10, 0)
  File "main.py", line 12, in divide
    return a / b
ZeroDivisionError: division by zero
```

这比只打印“出错了”强很多。

---

# 16. Logger、Handler、Formatter 是什么？🧠

这三个是 Python 日志的核心。

## Logger：日志记录器

负责产生日志。

```python
logger = logging.getLogger(__name__)
logger.info("程序启动")
```

## Handler：日志处理器

负责把日志发到哪里。

比如：

* 控制台
* 文件
* 网络
* 邮件

官方文档说明，Handler 会根据日志严重程度把日志派发到指定目标，Logger 可以添加 0 个或多个 Handler；同一个程序可以把不同级别日志发到不同目标。([Python documentation][1])

## Formatter：日志格式器

负责日志长什么样。

```python
formatter = logging.Formatter(
    "%(asctime)s [%(levelname)s] %(name)s:%(lineno)d - %(message)s"
)
```

官方文档也说明，Formatter 负责配置日志消息最终的顺序、结构和内容。([Python documentation][1])

---

# 17. Java 类比一下 ☕🐍

你如果学过 Java，可以这样类比：

| Java / Logback | Python logging    |
| -------------- | ----------------- |
| Logger         | Logger            |
| Appender       | Handler           |
| Pattern/Layout | Formatter         |
| logback.xml    | dictConfig / 配置文件 |
| log.info(...)  | logger.info(...)  |
| log.error(...) | logger.error(...) |

所以 Python 日志并不奇怪，本质就是：

> **Logger 产生日志，Handler 决定发到哪里，Formatter 决定长什么样。**

---

# 18. 同时输出到控制台和文件 🖥️📂

这是很常见的需求。

```python
import logging

logger = logging.getLogger(__name__)
logger.setLevel(logging.DEBUG)

formatter = logging.Formatter(
    "%(asctime)s [%(levelname)s] %(name)s:%(lineno)d - %(message)s",
    datefmt="%Y-%m-%d %H:%M:%S"
)

console_handler = logging.StreamHandler()
console_handler.setLevel(logging.INFO)
console_handler.setFormatter(formatter)

file_handler = logging.FileHandler("app.log", encoding="utf-8")
file_handler.setLevel(logging.DEBUG)
file_handler.setFormatter(formatter)

logger.addHandler(console_handler)
logger.addHandler(file_handler)

logger.debug("这条只进文件")
logger.info("这条进控制台和文件")
logger.error("错误信息")
```

这里的效果是：

* `DEBUG` 进文件
* `INFO` 及以上进控制台
* `INFO` 及以上也进文件

Logging Cookbook 中也展示了类似配置：文件 handler 记录 `DEBUG` 及以上，控制台 handler 记录 `INFO` 及以上；同一条日志可以被多个 handler 处理。([Python documentation][2])

---

# 19. logger 的 level 和 handler 的 level 有什么区别？🚪

这块一开始很容易懵。

看这段：

```python
logger.setLevel(logging.DEBUG)
console_handler.setLevel(logging.INFO)
file_handler.setLevel(logging.DEBUG)
```

意思是：

## logger 先过滤一遍

```python
logger.setLevel(logging.DEBUG)
```

表示 logger 愿意接收 `DEBUG` 及以上。

## handler 再过滤一遍

```python
console_handler.setLevel(logging.INFO)
```

表示控制台只输出 `INFO` 及以上。

```python
file_handler.setLevel(logging.DEBUG)
```

表示文件输出 `DEBUG` 及以上。

官方文档也明确说明，logger 的 level 决定哪些严重程度的日志会传给 handler，而 handler 的 level 决定这个 handler 最终发送哪些日志。([Python documentation][1])

你可以这样记：

> **logger 是第一道门，handler 是第二道门。** 🚪🚪

---

# 20. 日志文件太大怎么办？RotatingFileHandler 🔄

如果一直写：

```python
app.log
```

时间久了文件可能特别大。

可以用滚动日志：

```python
import logging
from logging.handlers import RotatingFileHandler

logger = logging.getLogger(__name__)
logger.setLevel(logging.INFO)

formatter = logging.Formatter(
    "%(asctime)s [%(levelname)s] %(name)s:%(lineno)d - %(message)s"
)

handler = RotatingFileHandler(
    "app.log",
    maxBytes=1024 * 1024,
    backupCount=5,
    encoding="utf-8"
)
handler.setFormatter(formatter)

logger.addHandler(handler)

logger.info("程序启动")
```

这里：

```python
maxBytes=1024 * 1024
```

表示单个日志文件约 1MB。

```python
backupCount=5
```

表示保留 5 个备份文件。

Python 的 `RotatingFileHandler` 支持按文件大小滚动日志；当日志文件接近 `maxBytes` 时会滚动，`backupCount` 非 0 时会保留旧日志，例如 `app.log.1`、`app.log.2` 等。([Python documentation][4])

---

# 21. 一个适合你现在用的项目级日志工具 🧰

可以建一个文件：

```text
logger_config.py
```

内容：

```python
import logging
from logging.handlers import RotatingFileHandler
from pathlib import Path


def setup_logging() -> None:
    log_dir = Path("logs")
    log_dir.mkdir(exist_ok=True)

    log_format = "%(asctime)s [%(levelname)s] %(name)s:%(lineno)d - %(message)s"

    logging.basicConfig(
        level=logging.INFO,
        format=log_format,
        datefmt="%Y-%m-%d %H:%M:%S",
        handlers=[
            logging.StreamHandler(),
            RotatingFileHandler(
                log_dir / "app.log",
                maxBytes=1024 * 1024,
                backupCount=5,
                encoding="utf-8"
            )
        ]
    )
```

然后在 `main.py` 里：

```python
import logging
from logger_config import setup_logging

setup_logging()

logger = logging.getLogger(__name__)


def main() -> None:
    logger.info("程序启动")

    try:
        result = 10 / 0
        logger.info("计算结果 result=%s", result)
    except ZeroDivisionError:
        logger.exception("计算失败")


if __name__ == "__main__":
    main()
```

项目结构：

```text
python_demo/
├── main.py
├── logger_config.py
└── logs/
    └── app.log
```

这已经是一个很实用的入门模板了 😄

---

# 22. 常见坑：重复打印日志 😵

有时候你会看到同一条日志打印两遍。

常见原因是：

```python
logger.addHandler(handler)
logger.addHandler(handler)
```

或者某个配置函数被调用了多次，重复添加 handler。

可以简单防一下：

```python
if not logger.handlers:
    logger.addHandler(handler)
```

例如：

```python
logger = logging.getLogger(__name__)
logger.setLevel(logging.INFO)

if not logger.handlers:
    handler = logging.StreamHandler()
    logger.addHandler(handler)
```

不过在实际项目里，更推荐统一在程序入口配置一次日志，不要每个模块都重复配置。

---

# 23. 常见坑：`basicConfig()` 不生效 🤯

如果你写：

```python
logging.info("先打了一条日志")

logging.basicConfig(level=logging.DEBUG)

logging.debug("debug 信息")
```

可能发现 `DEBUG` 还是不出来。

原因是 `basicConfig()` 应该尽早调用。官方教程也提醒，`basicConfig()` 应该在调用 `debug()`、`info()` 等日志方法之前调用，否则日志事件可能不会按预期处理。([Python documentation][1])

所以正确习惯是：

```python
import logging

logging.basicConfig(level=logging.DEBUG)

logger = logging.getLogger(__name__)
logger.debug("debug 信息")
```

---

# 24. 常见坑：用 `warn` 而不是 `warning` 🚫

不要写：

```python
logger.warn("不推荐")
```

要写：

```python
logger.warning("推荐")
```

官方文档说明 `warn` 是过时方法，功能上和 `warning` 一样，但已经 deprecated，因此应使用 `warning`。([Python documentation][3])

---

# 25. 常见坑：日志里不要乱放敏感信息 🔐

比如这些不要直接打：

```text
密码
token
身份证
银行卡
手机号完整值
数据库密码
access_key
secret_key
```

错误示例：

```python
logger.info("用户登录 password=%s", password)
```

更好的写法：

```python
logger.info("用户登录 username=%s", username)
```

日志是为了排查问题，不是为了把隐私数据全存下来。

---

# 26. 日志应该写什么？📌

比较推荐记录这些：

## 程序启动/结束

```python
logger.info("程序启动")
logger.info("程序结束")
```

## 关键业务操作

```python
logger.info("开始导入文件 filename=%s", filename)
logger.info("文件导入完成 total=%s success=%s fail=%s", total, success, fail)
```

## 参数摘要

```python
logger.debug("查询参数 start_date=%s end_date=%s", start_date, end_date)
```

## 异常堆栈

```python
logger.exception("任务执行失败 task_id=%s", task_id)
```

## 外部依赖调用结果

```python
logger.info("调用接口完成 status_code=%s cost_ms=%s", status_code, cost_ms)
```

---

# 27. 日志不应该写什么？🚫

不推荐：

```python
logger.info("进入函数")
logger.info("执行到这里")
logger.info("aaaa")
logger.info("test")
```

这种日志太空，没有排查价值。

更好的写法：

```python
logger.info("开始处理订单 order_id=%s", order_id)
logger.info("订单处理完成 order_id=%s status=%s", order_id, status)
```

有对象、有状态、有结果，才有价值。

---

# 28. 一个完整小案例：文件导入日志 📂

这个例子贴近你以后做数据处理、CSV 上传、分析系统。

```python
import csv
import logging
from pathlib import Path

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(name)s:%(lineno)d - %(message)s",
    datefmt="%Y-%m-%d %H:%M:%S",
    filename="import.log",
    encoding="utf-8"
)

logger = logging.getLogger(__name__)


def import_students(file_path: str) -> None:
    path = Path(file_path)

    if not path.exists():
        logger.error("文件不存在 file_path=%s", file_path)
        return

    total = 0
    success = 0
    fail = 0

    logger.info("开始导入学生文件 file_path=%s", file_path)

    try:
        with path.open("r", encoding="utf-8", newline="") as file:
            reader = csv.DictReader(file)

            for row in reader:
                total += 1

                try:
                    name = row["name"]
                    age = int(row["age"])
                    score = float(row["score"])

                    logger.debug(
                        "解析学生数据成功 name=%s age=%s score=%s",
                        name,
                        age,
                        score
                    )
                    success += 1

                except (KeyError, ValueError):
                    logger.exception("解析学生数据失败 row=%s", row)
                    fail += 1

    except OSError:
        logger.exception("读取文件失败 file_path=%s", file_path)
        return

    logger.info(
        "学生文件导入完成 total=%s success=%s fail=%s",
        total,
        success,
        fail
    )


if __name__ == "__main__":
    import_students("students.csv")
```

这个案例包含了：

* 文件是否存在
* 开始导入
* 每行解析
* 单行失败记录
* 文件读取失败
* 最终统计结果

这比 `print()` 专业很多 🚀

---

# 29. 初学阶段你先掌握这几个就够了 ✅

你现在不需要一口气学完 logging 的所有高级功能。先掌握：

```python
import logging
```

```python
logging.basicConfig(...)
```

```python
logger = logging.getLogger(__name__)
```

```python
logger.debug(...)
logger.info(...)
logger.warning(...)
logger.error(...)
logger.exception(...)
logger.critical(...)
```

以及三个概念：

```text
Logger      产生日志
Handler     输出到哪里
Formatter   日志长什么样
```

---

# 30. 最后总结一下 🎯

Python 日志的核心就是：

```text
日志级别：DEBUG / INFO / WARNING / ERROR / CRITICAL
日志对象：logger = logging.getLogger(__name__)
日志配置：logging.basicConfig(...)
日志输出：控制台 / 文件 / 滚动文件
异常记录：logger.exception(...)
```

你以后写项目，最推荐的基础模板是：

```python
import logging

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(name)s:%(lineno)d - %(message)s",
    datefmt="%Y-%m-%d %H:%M:%S"
)

logger = logging.getLogger(__name__)

logger.info("程序启动")
```

一句话记住：

> **学习时可以用 `print()`，写项目要用 `logging`。** 🐍📝

[1]: https://docs.python.org/3/howto/logging.html "Logging HOWTO — Python 3.14.5rc1 documentation"
[2]: https://docs.python.org/3/howto/logging-cookbook.html "Logging Cookbook — Python 3.14.5rc1 documentation"
[3]: https://docs.python.org/3/library/logging.html "logging — Logging facility for Python — Python 3.14.5rc1 documentation"
[4]: https://docs.python.org/3/library/logging.handlers.html "logging.handlers — Logging handlers — Python 3.14.5rc1 documentation"
