# 1. 先用人话理解同步和异步 🤔

## 同步：一个一个来

比如你要做三件事：

```text
烧水 3 秒
煮面 3 秒
洗碗 3 秒
```

同步写法就是：

```text
先烧水，等烧完
再煮面，等煮完
再洗碗，等洗完
```

总共大约：

```text
3 + 3 + 3 = 9 秒
```

它的特点是：

> **前一个任务没结束，后一个任务不能开始。**

---

## 异步：等待的时候切去做别的事 ⚡

异步更像：

```text
开始烧水
烧水等待期间，去洗碗
洗碗等待期间，去准备面
水好了，再回来继续煮面
```

它不是说你有三个人同时干活，而是：

> **一个任务在等待时，把控制权让出来，让别的任务先执行。**

所以异步很适合：

* HTTP 请求 🌐
* 数据库查询 🗄️
* 文件读写 📂
* 网络连接 🔌
* 等待外部接口返回 ⏳

---

# 2. 异步不是多线程，也不是一定更快 ⚠️

这个一定要先说清楚。

Python 异步编程主要解决的是：

> **I/O 等待多的问题。**

比如你同时请求 100 个接口，很多时间都在等服务器响应。异步就很有用。

但是如果你做的是：

* 大量数学计算
* 图片压缩
* 视频处理
* 机器学习训练
* CPU 密集计算

异步不一定有用。因为这些任务不是在“等”，而是在真正占用 CPU 算。

你可以这样记：

```text
网络请求多 → 异步很适合 ✅
等待数据库多 → 异步很适合 ✅
大量 CPU 计算 → 异步不一定适合 ❌
```

---

# 3. Python 异步核心关键词 🧩

Python 异步最核心的几个词：

```text
async
await
coroutine 协程
event loop 事件循环
task 任务
asyncio
```

先不用怕，我们一个个拆。

---

# 4. `async def`：定义异步函数 🐍

普通函数是这样：

```python
def hello():
    print("hello")
```

异步函数是这样：

```python
async def hello():
    print("hello")
```

多了一个：

```python
async
```

这表示：

> 这个函数是一个异步函数，也叫协程函数。

---

# 5. 调用异步函数不会立刻执行 ⚠️

这个是新手最容易懵的地方。

看代码：

```python
async def hello():
    print("hello")

hello()
```

你可能以为会输出：

```text
hello
```

但实际上，**不会这样执行**。

因为调用异步函数：

```python
hello()
```

得到的是一个 **协程对象 coroutine object**，不是直接执行函数内容。Python 官方文档也把 coroutines、awaitables、tasks 放在 `asyncio` 的核心概念里讲，并展示了异步函数需要通过 `await` 或 `asyncio.run()` 执行。([Python documentation][2])

正确写法是：

```python
import asyncio


async def hello():
    print("hello")


asyncio.run(hello())
```

输出：

```text
hello
```

---

# 6. `asyncio.run()`：启动异步程序 🚀

这个你可以先理解成：

> **异步程序的入口。**

完整例子：

```python
import asyncio


async def main():
    print("程序开始")
    print("程序结束")


asyncio.run(main())
```

输出：

```text
程序开始
程序结束
```

官方文档的 `asyncio` Hello World 示例也是定义 `async def main()`，然后用 `asyncio.run(main())` 启动。([Python documentation][1])

你可以先记住：

```python
asyncio.run(main())
```

类似于：

> 把异步函数真正跑起来。

---

# 7. `await` 是什么？⭐

`await` 是异步编程里最重要的关键字。

你可以先把它理解成：

> **等待一个异步操作完成，并且在等待期间允许程序去执行别的任务。**

比如：

```python
import asyncio


async def main():
    print("开始")
    await asyncio.sleep(2)
    print("结束")


asyncio.run(main())
```

输出：

```text
开始
等待约 2 秒
结束
```

这里：

```python
await asyncio.sleep(2)
```

不是普通睡眠，而是异步等待。

意思是：

> 这个协程先暂停 2 秒，在这 2 秒里事件循环可以去执行别的任务。

Python 官方文档里也用 `await asyncio.sleep(1)` 展示异步等待。([Python documentation][1])

---

# 8. `time.sleep()` 和 `asyncio.sleep()` 的区别 😵

这个特别重要。

## 同步阻塞睡眠

```python
import time

time.sleep(2)
```

意思是：

> 当前线程直接睡死 2 秒，什么都干不了。

---

## 异步非阻塞等待

```python
import asyncio

await asyncio.sleep(2)
```

意思是：

> 当前协程暂停 2 秒，把执行权让出来，事件循环可以先跑别的协程。

所以在异步函数里，不要乱用：

```python
time.sleep(2)
```

要用：

```python
await asyncio.sleep(2)
```

---

# 9. 第一个异步并发例子 🌟

先看同步思维：

```python
import asyncio


async def task1():
    print("任务1开始")
    await asyncio.sleep(2)
    print("任务1结束")


async def task2():
    print("任务2开始")
    await asyncio.sleep(2)
    print("任务2结束")


async def main():
    await task1()
    await task2()


asyncio.run(main())
```

这个大约需要 4 秒。

因为：

```python
await task1()
await task2()
```

意思是：

> 等 task1 完成，再执行 task2。

这虽然用了 async / await，但并没有真正并发。

---

# 10. 真正并发：`asyncio.create_task()` 🚀

```python
import asyncio


async def task1():
    print("任务1开始")
    await asyncio.sleep(2)
    print("任务1结束")


async def task2():
    print("任务2开始")
    await asyncio.sleep(2)
    print("任务2结束")


async def main():
    t1 = asyncio.create_task(task1())
    t2 = asyncio.create_task(task2())

    await t1
    await t2


asyncio.run(main())
```

这次大约只需要 2 秒。

因为：

```python
t1 = asyncio.create_task(task1())
t2 = asyncio.create_task(task2())
```

会把两个协程包装成任务，并安排到事件循环里运行。Python 官方的 asyncio 概念文档也说，推荐用 `asyncio.create_task()` 创建任务，创建任务会自动把它调度到事件循环中执行。([Python documentation][3])

---

# 11. `await task1()` 和 `create_task(task1())` 的区别 🧠

这个你一定要理解。

## 写法 1：直接 await

```python
await task1()
await task2()
```

意思是：

```text
等 task1 完成
再开始 task2
```

这是顺序执行。

---

## 写法 2：create_task

```python
t1 = asyncio.create_task(task1())
t2 = asyncio.create_task(task2())

await t1
await t2
```

意思是：

```text
先把 task1 安排上
再把 task2 安排上
然后等待它们完成
```

这是并发执行。

---

# 12. 更简单的并发写法：`asyncio.gather()` 📦

如果你要同时执行多个任务，常用：

```python
import asyncio


async def download(name: str, seconds: int) -> str:
    print(f"{name} 开始下载")
    await asyncio.sleep(seconds)
    print(f"{name} 下载完成")
    return f"{name} result"


async def main():
    results = await asyncio.gather(
        download("文件A", 2),
        download("文件B", 3),
        download("文件C", 1),
    )

    print(results)


asyncio.run(main())
```

输出大概是：

```text
文件A 开始下载
文件B 开始下载
文件C 开始下载
文件C 下载完成
文件A 下载完成
文件B 下载完成
['文件A result', '文件B result', '文件C result']
```

`asyncio.gather()` 用来并发运行多个 awaitable，并收集结果；官方文档把它放在“Running tasks concurrently”这一类高层 API 里。([Python documentation][2])

---

# 13. `gather()` 的返回结果顺序 📌

注意：

```python
results = await asyncio.gather(
    download("文件A", 2),
    download("文件B", 3),
    download("文件C", 1),
)
```

虽然 `文件C` 最先完成，但 `results` 的顺序还是按照你传入的顺序：

```python
[
    "文件A result",
    "文件B result",
    "文件C result"
]
```

也就是说：

> **谁先完成不影响结果列表顺序。**

---

# 14. Python 3.11+ 更推荐的结构：`TaskGroup` 🧰

如果你的 Python 版本是 3.11 或以上，可以认识一下 `asyncio.TaskGroup`。

```python
import asyncio


async def worker(name: str, seconds: int) -> None:
    print(f"{name} 开始")
    await asyncio.sleep(seconds)
    print(f"{name} 结束")


async def main():
    async with asyncio.TaskGroup() as tg:
        tg.create_task(worker("任务A", 2))
        tg.create_task(worker("任务B", 1))
        tg.create_task(worker("任务C", 3))

    print("所有任务都结束了")


asyncio.run(main())
```

`TaskGroup` 是一个异步上下文管理器，用来管理一组任务；官方高层 API 索引也说明它可以持有一组任务，并提供一种方便可靠的方式等待组内任务完成。([Python documentation][4])

你现在不用急着完全掌握它，先记住：

```text
简单并发：asyncio.gather()
结构化并发：asyncio.TaskGroup()
```

---

# 15. 事件循环 event loop 是什么？🔁

事件循环你可以理解成：

> **异步程序的调度器。**

它会不断做这几件事：

```text
看看哪个任务可以执行
执行一会儿
遇到 await，就暂停当前任务
切换去执行别的任务
等 I/O 好了，再回来继续
```

可以想象成一个“任务调度员”：

```text
任务A：等网络返回 → 先挂起
任务B：可以继续执行 → 跑任务B
任务C：等数据库返回 → 先挂起
任务A：网络返回了 → 继续执行任务A
```

这就是异步的核心。

---

# 16. 协程 coroutine 是什么？🧩

协程就是一种可以暂停、恢复的函数。

普通函数：

```python
def func():
    print("开始")
    print("结束")
```

它一旦开始执行，就一路跑完。

异步函数：

```python
async def func():
    print("开始")
    await asyncio.sleep(1)
    print("结束")
```

它可以在：

```python
await asyncio.sleep(1)
```

这里暂停，把控制权交出去，后面再恢复执行。

所以你可以这样记：

> **协程 = 可以主动让出执行权的函数。**

---

# 17. Java 思维类比一下 ☕🐍

你有 Java 背景，可以粗略这样对照：

| Java                        | Python                     |
| --------------------------- | -------------------------- |
| `CompletableFuture`         | `asyncio.Task` / coroutine |
| `thenApply` / `thenCompose` | `await` 后继续执行              |
| 线程池调度任务                     | event loop 调度协程            |
| WebClient 异步请求              | httpx.AsyncClient          |
| 阻塞等待 `get()`                | `await`                    |

但要注意：

> Python 的 async / await 默认不是线程池模型，它更像是单线程事件循环下的协作式并发。

---

# 18. 异步 HTTP 请求：用 httpx.AsyncClient 🌐

你前面学了 HTTP 请求，用的是 `requests`。
`requests` 是同步的，写在异步函数里会阻塞。

异步 HTTP 请求更推荐用：

```bash
python -m pip install httpx
```

HTTPX 官方文档说明它同时支持同步和异步 API，并支持 HTTP/1.1 和 HTTP/2；异步支持页面也说明 HTTPX 支持 asyncio 或 trio 作为异步环境。([HTTPX][5])

---

## 18.1 单个异步请求

```python
import asyncio

import httpx


async def fetch_url(url: str) -> dict:
    async with httpx.AsyncClient() as client:
        response = await client.get(url, timeout=10)
        response.raise_for_status()
        return response.json()


async def main():
    data = await fetch_url("https://api.github.com/events")
    print(type(data))
    print(data[0])


asyncio.run(main())
```

注意两个点：

```python
async with httpx.AsyncClient() as client:
```

这是异步上下文管理器。

```python
response = await client.get(...)
```

异步请求要 `await`。

---

## 18.2 多个异步请求并发执行

```python
import asyncio

import httpx


async def fetch_url(client: httpx.AsyncClient, url: str) -> tuple[str, int]:
    response = await client.get(url, timeout=10)
    return url, response.status_code


async def main():
    urls = [
        "https://api.github.com",
        "https://www.python.org",
        "https://www.example.com",
    ]

    async with httpx.AsyncClient() as client:
        tasks = [
            fetch_url(client, url)
            for url in urls
        ]

        results = await asyncio.gather(*tasks)

    for url, status_code in results:
        print(url, status_code)


asyncio.run(main())
```

这里：

```python
tasks = [
    fetch_url(client, url)
    for url in urls
]
```

创建了一组协程。

```python
results = await asyncio.gather(*tasks)
```

并发等待它们完成。

---

# 19. 为什么不要在 async 函数里直接用 requests？⚠️

不要这样：

```python
import requests


async def fetch():
    response = requests.get("https://api.github.com/events")
    return response.json()
```

虽然语法上不一定报错，但 `requests.get()` 是同步阻塞调用。它会卡住当前事件循环，让别的协程没法运行。

异步函数里更推荐：

```python
import httpx


async def fetch():
    async with httpx.AsyncClient() as client:
        response = await client.get("https://api.github.com/events")
        return response.json()
```

---

# 20. 异步里常见错误 1：忘记 await 🚨

错误写法：

```python
async def get_data():
    return 123


async def main():
    result = get_data()
    print(result)
```

输出可能是一个 coroutine 对象，而不是 `123`。

正确写法：

```python
async def get_data():
    return 123


async def main():
    result = await get_data()
    print(result)
```

记住：

> **调用 async 函数，通常需要 await。**

---

# 21. 常见错误 2：在普通函数里用 await 🚫

错误：

```python
def main():
    await asyncio.sleep(1)
```

这会报错。

因为：

```python
await
```

只能写在：

```python
async def
```

里面。

正确：

```python
async def main():
    await asyncio.sleep(1)
```

---

# 22. 常见错误 3：Jupyter / PyCharm 控制台里 asyncio.run 报错 😵

有些环境里本身已经有事件循环，比如 Jupyter Notebook。

你可能会遇到：

```text
RuntimeError: asyncio.run() cannot be called from a running event loop
```

普通 `.py` 文件里推荐：

```python
asyncio.run(main())
```

但在某些交互式环境里，可能直接：

```python
await main()
```

就可以。

---

# 23. 常见错误 4：异步函数里用了阻塞代码 🧱

比如：

```python
async def task():
    time.sleep(3)
```

这会阻塞事件循环。

应该改成：

```python
async def task():
    await asyncio.sleep(3)
```

再比如同步 HTTP：

```python
requests.get(url)
```

异步里更推荐：

```python
await client.get(url)
```

---

# 24. 异步异常处理 🧯

和普通异常类似：

```python
import asyncio


async def risky_task(name: str) -> str:
    await asyncio.sleep(1)

    if name == "B":
        raise ValueError("任务 B 出错了")

    return f"{name} 完成"


async def main():
    try:
        results = await asyncio.gather(
            risky_task("A"),
            risky_task("B"),
            risky_task("C"),
        )
        print(results)
    except ValueError as e:
        print("捕获异常：", e)


asyncio.run(main())
```

异步代码也需要异常处理，尤其是 HTTP 请求、数据库调用、文件操作这些地方。

---

# 25. 如果想让部分任务失败也继续？🧩

`gather()` 有个参数：

```python
return_exceptions=True
```

例子：

```python
import asyncio


async def task(name: str) -> str:
    await asyncio.sleep(1)

    if name == "B":
        raise ValueError("B 失败")

    return f"{name} 成功"


async def main():
    results = await asyncio.gather(
        task("A"),
        task("B"),
        task("C"),
        return_exceptions=True
    )

    for result in results:
        if isinstance(result, Exception):
            print("任务失败：", result)
        else:
            print("任务成功：", result)


asyncio.run(main())
```

输出大概：

```text
任务成功： A 成功
任务失败： B 失败
任务成功： C 成功
```

适合这种场景：

> 批量请求 100 个接口，其中几个失败了，不希望整体直接崩掉。

---

# 26. 控制并发数量：Semaphore 🚦

如果你同时发 1000 个请求，可能把对方服务器打爆，也可能把自己电脑资源耗尽。

可以用 `asyncio.Semaphore` 控制并发数量。

```python
import asyncio

import httpx


async def fetch(
    client: httpx.AsyncClient,
    sem: asyncio.Semaphore,
    url: str
) -> tuple[str, int]:
    async with sem:
        response = await client.get(url, timeout=10)
        return url, response.status_code


async def main():
    urls = [
        "https://www.example.com"
        for _ in range(20)
    ]

    sem = asyncio.Semaphore(5)

    async with httpx.AsyncClient() as client:
        tasks = [
            fetch(client, sem, url)
            for url in urls
        ]

        results = await asyncio.gather(*tasks)

    print(results)


asyncio.run(main())
```

这里：

```python
sem = asyncio.Semaphore(5)
```

表示最多同时跑 5 个请求。

这在爬虫、批量 API 调用里特别常用 🕷️🌐

---

# 27. 超时控制：asyncio.timeout ⏰

如果你希望某段异步操作最多运行几秒，可以用超时。

```python
import asyncio


async def slow_task():
    await asyncio.sleep(5)
    return "完成"


async def main():
    try:
        async with asyncio.timeout(2):
            result = await slow_task()
            print(result)
    except TimeoutError:
        print("任务超时")


asyncio.run(main())
```

`asyncio` 高层 API 索引里也列出了 timeout、wait_for、waiting primitives 等用于超时和等待控制的 API。([Python documentation][4])

---

# 28. 异步队列：asyncio.Queue 📮

如果你有“生产者-消费者”模型，可以用队列。

比如：

```text
生产者：不断产生 URL
消费者：不断请求 URL
```

示例：

```python
import asyncio


async def producer(queue: asyncio.Queue[str]) -> None:
    for i in range(5):
        url = f"https://example.com/page/{i}"
        print("生产：", url)
        await queue.put(url)

    await queue.put("STOP")


async def consumer(queue: asyncio.Queue[str]) -> None:
    while True:
        url = await queue.get()

        if url == "STOP":
            break

        print("消费：", url)
        await asyncio.sleep(1)


async def main():
    queue: asyncio.Queue[str] = asyncio.Queue()

    await asyncio.gather(
        producer(queue),
        consumer(queue)
    )


asyncio.run(main())
```

队列适合更复杂的异步任务编排，比如爬虫、任务处理、消息消费。

---

# 29. 异步和多线程怎么选？🧭

简单记：

| 场景         | 建议                 |
| ---------- | ------------------ |
| 大量 HTTP 请求 | 异步                 |
| 大量数据库 I/O  | 异步                 |
| 大量文件/网络等待  | 异步                 |
| CPU 密集计算   | 多进程 / C 扩展 / 专门计算库 |
| 调用阻塞第三方库   | 线程池或换异步库           |
| 简单脚本       | 同步就够了              |

不要为了异步而异步。

如果只是写一个简单脚本：

```python
requests.get(url)
```

完全没问题。

如果你要批量请求 100 个接口：

```python
httpx.AsyncClient + asyncio.gather
```

就很合适。

---

# 30. 一个完整小项目：异步批量请求 URL 🧪

这个例子把前面内容串起来：

```python
import asyncio
import logging
from dataclasses import dataclass

import httpx

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(name)s:%(lineno)d - %(message)s",
    datefmt="%Y-%m-%d %H:%M:%S"
)

logger = logging.getLogger(__name__)


@dataclass
class FetchResult:
    url: str
    status_code: int | None
    error: str | None = None


async def fetch_url(
    client: httpx.AsyncClient,
    sem: asyncio.Semaphore,
    url: str
) -> FetchResult:
    async with sem:
        try:
            logger.info("开始请求 url=%s", url)

            response = await client.get(url, timeout=10)

            logger.info(
                "请求完成 url=%s status_code=%s",
                url,
                response.status_code
            )

            return FetchResult(
                url=url,
                status_code=response.status_code
            )

        except httpx.HTTPError as e:
            logger.exception("请求失败 url=%s", url)

            return FetchResult(
                url=url,
                status_code=None,
                error=str(e)
            )


async def main() -> None:
    urls = [
        "https://www.python.org",
        "https://www.example.com",
        "https://api.github.com",
    ]

    sem = asyncio.Semaphore(3)

    async with httpx.AsyncClient() as client:
        tasks = [
            fetch_url(client, sem, url)
            for url in urls
        ]

        results = await asyncio.gather(*tasks)

    for result in results:
        print(result)


if __name__ == "__main__":
    asyncio.run(main())
```

这个例子里用了：

* `async def`
* `await`
* `asyncio.run`
* `asyncio.gather`
* `asyncio.Semaphore`
* `httpx.AsyncClient`
* `dataclass`
* `typing`
* `logging`
* 异常处理

很适合作为你异步编程的第一个完整模板 ✅

---

# 31. 你现在应该怎么学异步？🪜

建议顺序是：

```text
1. 先理解 async def 和 await
2. 再理解 asyncio.run()
3. 再理解 create_task()
4. 再学 gather()
5. 再学异步 HTTP：httpx.AsyncClient
6. 再学异常处理和超时
7. 再学 Semaphore 控制并发
8. 最后学 Queue / TaskGroup
```

不要一开始就上复杂项目，不然容易绕晕 😵

---

# 32. 练习题来一组 ✍️🔥

## 练习 1：最简单异步函数

写一个异步函数：

```python
async def say_hello():
    ...
```

要求：

```text
打印 开始
等待 1 秒
打印 结束
```

---

## 练习 2：顺序执行两个任务

写两个异步任务，每个等待 2 秒。
用：

```python
await task1()
await task2()
```

观察总耗时。

---

## 练习 3：并发执行两个任务

把上题改成：

```python
await asyncio.gather(task1(), task2())
```

观察总耗时。

---

## 练习 4：异步请求 3 个 URL

使用 `httpx.AsyncClient` 并发请求：

```text
https://www.python.org
https://www.example.com
https://api.github.com
```

打印状态码。

---

## 练习 5：限制并发数量

准备 20 个 URL，用：

```python
asyncio.Semaphore(5)
```

限制最多同时请求 5 个。

---

# 33. 练习参考答案 ✅

## 练习 1

```python
import asyncio


async def say_hello():
    print("开始")
    await asyncio.sleep(1)
    print("结束")


asyncio.run(say_hello())
```

---

## 练习 2

```python
import asyncio


async def task1():
    print("task1 开始")
    await asyncio.sleep(2)
    print("task1 结束")


async def task2():
    print("task2 开始")
    await asyncio.sleep(2)
    print("task2 结束")


async def main():
    await task1()
    await task2()


asyncio.run(main())
```

---

## 练习 3

```python
import asyncio


async def task1():
    print("task1 开始")
    await asyncio.sleep(2)
    print("task1 结束")


async def task2():
    print("task2 开始")
    await asyncio.sleep(2)
    print("task2 结束")


async def main():
    await asyncio.gather(
        task1(),
        task2()
    )


asyncio.run(main())
```

---

## 练习 4

```python
import asyncio

import httpx


async def fetch(client: httpx.AsyncClient, url: str) -> tuple[str, int]:
    response = await client.get(url, timeout=10)
    return url, response.status_code


async def main():
    urls = [
        "https://www.python.org",
        "https://www.example.com",
        "https://api.github.com",
    ]

    async with httpx.AsyncClient() as client:
        results = await asyncio.gather(
            *(fetch(client, url) for url in urls)
        )

    for url, status_code in results:
        print(url, status_code)


asyncio.run(main())
```

---

# 34. 最后总结 🎯

Python 异步编程你先抓住这些核心：

```python
async def main():
    ...
```

定义异步函数。

```python
await something()
```

等待异步操作。

```python
asyncio.run(main())
```

启动异步程序。

```python
asyncio.create_task(...)
```

创建并发任务。

```python
await asyncio.gather(...)
```

并发等待多个任务。

```python
httpx.AsyncClient()
```

发送异步 HTTP 请求。

一句话记住：

> **异步不是让 CPU 算得更快，而是让程序在等待网络、数据库、文件这些 I/O 时，不要傻等。** 🐍⚡

[1]: https://docs.python.org/3/library/asyncio.html?utm_source=chatgpt.com "asyncio — Asynchronous I/O"
[2]: https://docs.python.org/3/library/asyncio-task.html?utm_source=chatgpt.com "Coroutines and tasks"
[3]: https://docs.python.org/3/howto/a-conceptual-overview-of-asyncio.html?utm_source=chatgpt.com "A Conceptual Overview of asyncio"
[4]: https://docs.python.org/3/library/asyncio-api-index.html?utm_source=chatgpt.com "High-level API Index"
[5]: https://www.python-httpx.org/?utm_source=chatgpt.com "HTTPX"
