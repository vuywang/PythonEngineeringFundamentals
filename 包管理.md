# 1. 先搞清楚几个词 🧠

## 1.1 模块 module

一个 `.py` 文件就可以是一个模块。

比如：

```text
utils.py
```

里面写：

```python
def add(a, b):
    return a + b
```

别的文件可以：

```python
import utils

print(utils.add(1, 2))
```

---

## 1.2 包 package

一个文件夹里放 Python 代码，就可以组成包。

比如：

```text
my_app/
├── main.py
└── utils/
    ├── __init__.py
    └── math_util.py
```

使用：

```python
from utils.math_util import add
```

这里的 `utils` 就可以理解成一个包。

---

## 1.3 第三方包 third-party package

比如：

```text
requests
pandas
fastapi
django
numpy
```

这些不是 Python 标准库自带的，一般要通过 `pip install` 安装。

`pip` 是 Python 的包安装器，可以从 Python Package Index，也就是 PyPI，以及其他索引安装包。([pip][2])

---

# 2. Python 包管理到底管什么？📦

主要管这几件事：

```text
1. 安装包
2. 卸载包
3. 查看包
4. 固定版本
5. 导出依赖
6. 按依赖文件还原环境
7. 处理开发依赖
8. 处理项目打包发布
```

你现在最先掌握前 6 个就够了 ✅

---

# 3. 最核心工具：pip 🛠️

`pip` 是最基础、最常见的 Python 包安装工具。

安装一个包：

```bash
python -m pip install requests
```

Windows 上也可以：

```bash
py -m pip install requests
```

官方包装指南里的示例也是用 `python -m pip install requests` 或 Windows 的 `py -m pip install requests` 来安装包；安装时，pip 会下载目标包以及它依赖的其他包。([packaging.python.org][1])

---

# 4. 为什么推荐 `python -m pip`，而不是直接 `pip`？🤔

你会看到两种写法：

```bash
pip install requests
```

和：

```bash
python -m pip install requests
```

初学阶段建议你优先用：

```bash
python -m pip install requests
```

原因是它更明确：

> **用当前这个 python 对应的 pip 去安装包。**

如果你电脑里有多个 Python，比如 Python 3.10、3.11、3.12，直接敲 `pip` 有时候会装到你没想到的环境里 😵

---

# 5. 包一定要装在虚拟环境里吗？📁

建议是：

> **项目用的第三方包，优先装到当前项目的 `.venv` 里。**

也就是：

```text
my_project/
├── .venv/
├── main.py
└── requirements.txt
```

官方包装指南也建议在使用第三方包时使用虚拟环境；虚拟环境会让不同项目之间的包安装相互隔离，避免一个项目的依赖影响另一个项目。([packaging.python.org][1])

---

# 6. 标准流程：创建环境 + 安装包 🚀

比如你新建一个项目：

```bash
mkdir python_demo
cd python_demo
```

创建虚拟环境：

```bash
python -m venv .venv
```

Windows PowerShell 激活：

```powershell
.venv\Scripts\Activate.ps1
```

Windows CMD 激活：

```cmd
.venv\Scripts\activate.bat
```

macOS / Linux 激活：

```bash
source .venv/bin/activate
```

然后安装包：

```bash
python -m pip install requests
```

虚拟环境激活后，`python` 和 `pip` 会优先指向当前虚拟环境里的可执行文件；官方指南也说明，激活后 pip 会把包安装到这个虚拟环境中。([packaging.python.org][1])

---

# 7. 查看当前装了哪些包 👀

常用两个命令：

```bash
python -m pip list
```

和：

```bash
python -m pip freeze
```

## `pip list`

更适合人看：

```text
Package    Version
---------- -------
requests   2.32.5
urllib3    2.5.0
```

## `pip freeze`

更适合生成依赖文件：

```text
requests==2.32.5
urllib3==2.5.0
```

你可以这样记：

> **list 给人看，freeze 给机器还原环境。**

---

# 8. 安装指定版本 🔢

比如安装指定版本：

```bash
python -m pip install requests==2.32.3
```

意思是：

> 就安装 `requests` 的 `2.32.3` 版本。

也可以限制范围：

```bash
python -m pip install "requests>=2.0.0,<3.0.0"
```

官方包装指南说明，pip 可以使用版本说明符安装指定版本，也可以用类似 `>=2.0.0,<3.0.0` 的范围限制来安装符合条件的版本。([packaging.python.org][1])

---

# 9. 版本符号怎么看？🧩

常见写法：

```text
requests==2.32.3
```

表示必须等于这个版本。

```text
requests>=2.0.0
```

表示大于等于这个版本。

```text
requests<3.0.0
```

表示小于这个版本。

```text
requests>=2.0.0,<3.0.0
```

表示在 2.x 范围内。

```text
requests~=2.32
```

大概表示兼容 `2.32` 这一支版本。

新手最常用的是：

```text
==
>=
<
```

就够了。

---

# 10. 升级包 ⬆️

升级某个包：

```bash
python -m pip install --upgrade requests
```

或者简写：

```bash
python -m pip install -U requests
```

官方包装指南也展示了用 `--upgrade` 升级包的写法。([packaging.python.org][1])

---

# 11. 卸载包 🗑️

```bash
python -m pip uninstall requests
```

执行后会提示确认：

```text
Proceed (Y/n)?
```

输入：

```text
y
```

就会卸载。

---

# 12. 查看某个包的信息 🔍

```bash
python -m pip show requests
```

会看到类似：

```text
Name: requests
Version: 2.32.5
Location: ...
Requires: certifi, charset_normalizer, idna, urllib3
```

这里比较有用的是：

* `Version`：当前版本
* `Location`：装在哪里
* `Requires`：它依赖哪些包

---

# 13. `requirements.txt` 是什么？📄

这个非常重要。

`requirements.txt` 就是一个依赖清单。

比如：

```text
requests==2.32.3
fastapi==0.115.6
pandas==2.2.3
```

别人拿到你的项目后，只要执行：

```bash
python -m pip install -r requirements.txt
```

就能按清单安装依赖。

官方指南也说明，可以把依赖写进 `requirements.txt`，再通过 `pip install -r requirements.txt` 一次性安装这些包。([packaging.python.org][1])

---

# 14. 怎么生成 requirements.txt？🧾

在你的虚拟环境里执行：

```bash
python -m pip freeze > requirements.txt
```

这会把当前虚拟环境里的包版本写进去。

比如生成：

```text
certifi==2025.11.12
charset-normalizer==3.4.4
idna==3.11
requests==2.32.5
urllib3==2.5.0
```

注意这里不仅有你手动安装的 `requests`，还有它依赖的包。

---

# 15. requirements.txt 要不要提交 Git？✅

要提交。

但是 `.venv` 不要提交。

推荐 `.gitignore`：

```gitignore
.venv/
venv/
env/
__pycache__/
*.pyc
```

推荐提交：

```text
requirements.txt
```

不推荐提交：

```text
.venv/
```

官方包装指南也明确建议把虚拟环境目录从版本控制系统中排除。([packaging.python.org][1])

---

# 16. requirements.txt 的常见写法 📌

## 16.1 固定版本

```text
requests==2.32.3
```

优点：环境稳定。

缺点：升级要手动改。

---

## 16.2 限制范围

```text
requests>=2.32,<3.0
```

优点：允许小版本升级。

缺点：有时新版本可能带来兼容问题。

---

## 16.3 不写版本

```text
requests
```

优点：简单。

缺点：别人安装时可能拿到不同版本，项目表现可能不一致。

---

# 17. 初学阶段怎么写最好？🎯

建议你先用：

```bash
python -m pip freeze > requirements.txt
```

这样最省事。

等你以后项目更正式，再考虑手写一个更干净的依赖清单。

---

# 18. 直接依赖和间接依赖是什么？🧠

假设你执行：

```bash
python -m pip install requests
```

你真正想用的是：

```text
requests
```

这叫 **直接依赖**。

但是 `requests` 自己还需要：

```text
urllib3
certifi
idna
charset-normalizer
```

这些叫 **间接依赖**。

所以你 `pip freeze` 时，会看到一堆不是你手动安装的包。官方指南的 `requests` 安装示例也展示了安装 `requests` 时会同时安装它依赖的包。([packaging.python.org][1])

---

# 19. 包名和导入名不一定一样 ⚠️

这个坑很常见。

比如你安装：

```bash
python -m pip install beautifulsoup4
```

但是代码里导入：

```python
from bs4 import BeautifulSoup
```

包名叫 `beautifulsoup4`，导入名叫 `bs4`。

再比如安装：

```bash
python -m pip install pillow
```

代码里导入：

```python
from PIL import Image
```

所以你要知道：

> **pip install 的名字，不一定等于 import 的名字。**

---

# 20. 开发依赖是什么？🧪

有些包不是程序运行必须的，而是开发时用的。

比如：

```text
pytest
ruff
mypy
black
```

它们一般用于：

* 测试
* 代码格式化
* 静态检查
* 类型检查

比如：

```bash
python -m pip install pytest
python -m pip install ruff
```

这些就是开发依赖。

---

# 21. Python 传统项目的依赖管理方式 🧱

最经典、最简单的是：

```text
.venv + pip + requirements.txt
```

也就是：

```bash
python -m venv .venv
.venv\Scripts\Activate.ps1
python -m pip install requests
python -m pip freeze > requirements.txt
```

这个组合适合：

* 初学练习
* 小项目
* 普通脚本
* 简单 Web 项目
* 毕设项目

你现在优先把这套用熟就行 😄

---

# 22. pyproject.toml 是什么？📦

稍微现代一点的 Python 项目，经常会有：

```text
pyproject.toml
```

它可以记录项目元数据、依赖、构建系统、工具配置等内容。

官方包装指南说明，`[project]` 表通常用于指定项目基本元数据，例如依赖、项目名称等；`[tool]` 表则给 black、mypy、hatch 等工具存放各自的配置。([packaging.python.org][3])

---

# 23. 一个简单的 pyproject.toml 长这样 👀

```toml
[project]
name = "python-demo"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.32,<3.0",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
]
```

这里：

```toml
dependencies = [
    "requests>=2.32,<3.0",
]
```

表示项目运行依赖。

```toml
[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
]
```

表示可选依赖，比如开发时使用。

官方文档也说明，`dependencies` 可以列出项目依赖，`optional-dependencies` 可以列出某些功能才需要的可选依赖，并且可以通过类似 `pip install your-project-name[gui]` 的方式安装额外依赖。([packaging.python.org][3])

---

# 24. requirements.txt 和 pyproject.toml 有什么区别？🤔

你可以这样理解：

| 文件                 | 主要作用           | 适合场景                 |
| ------------------ | -------------- | -------------------- |
| `requirements.txt` | 记录要安装哪些包       | 简单项目、部署、快速还原环境       |
| `pyproject.toml`   | 描述整个 Python 项目 | 正式项目、库开发、工具配置、现代项目管理 |
| lock 文件            | 锁定完整依赖版本       | 需要更稳定复现环境的项目         |

简单说：

> **requirements.txt 像依赖清单。**
> **pyproject.toml 像项目说明书。**
> **lock 文件像精确到版本的安装快照。**

---

# 25. 那你现在该用哪个？✅

以你现在学习阶段，我建议：

## 普通学习项目

用：

```text
.venv
requirements.txt
```

就够了。

## 稍微正式一点的项目

可以用：

```text
.venv
pyproject.toml
requirements.txt
```

## 更现代的项目

可以用：

```text
uv
pyproject.toml
uv.lock
```

---

# 26. 现在很火的 uv 是什么？⚡

`uv` 是一个比较新的 Python 包和项目管理工具，官方文档介绍它是用 Rust 写的、速度很快的 Python package and project manager，并且支持项目依赖、环境、lockfile、Python 版本、工具运行等功能。([Astral Docs][4])

比如：

```bash
uv init demo
cd demo
uv add requests
uv run python main.py
```

`uv add ruff` 这类命令会创建虚拟环境、解析并安装依赖，官方文档也展示了 `uv init`、`uv add`、`uv run`、`uv lock`、`uv sync` 等项目管理流程。([Astral Docs][4])

---

# 27. uv 和 pip 有什么区别？🧩

你可以先这样理解：

## pip

偏基础工具：

```text
安装包
卸载包
按 requirements.txt 安装
```

## uv

偏一体化工具：

```text
创建项目
管理虚拟环境
添加依赖
生成 lock 文件
同步环境
运行脚本
管理 Python 版本
```

不过初学阶段不要一上来被工具绕晕。
你的主线应该是：

> 先会 `pip + venv + requirements.txt`，再学 `uv`。

---

# 28. 包管理和虚拟环境的关系 🌱

虚拟环境解决的是：

> **包装在哪里？**

包管理解决的是：

> **装什么包？装什么版本？怎么记录？怎么还原？**

所以完整逻辑是：

```text
虚拟环境：隔离项目环境
pip：安装第三方包
requirements.txt：记录当前依赖
pyproject.toml：描述项目和依赖
lock 文件：锁定精确版本
```

---

# 29. 一个完整实战：用 pip 管理项目依赖 🧪

假设项目叫：

```text
weather_demo
```

创建项目：

```bash
mkdir weather_demo
cd weather_demo
```

创建虚拟环境：

```bash
python -m venv .venv
```

激活：

```powershell
.venv\Scripts\Activate.ps1
```

安装依赖：

```bash
python -m pip install requests pandas
```

写代码 `main.py`：

```python
import requests
import pandas as pd

print("requests version:", requests.__version__)
print("pandas version:", pd.__version__)
```

导出依赖：

```bash
python -m pip freeze > requirements.txt
```

以后别人拿到项目后：

```bash
python -m venv .venv
.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
python main.py
```

---

# 30. requirements.txt 的一个坑：环境可能太脏 😵

如果你在一个用了很久的虚拟环境里执行：

```bash
python -m pip freeze > requirements.txt
```

它可能导出很多当前项目根本没用到的包。

所以最好养成这个习惯：

> **一个项目一个干净 `.venv`。**

这样 `pip freeze` 出来的东西才不会太乱。

---

# 31. 另一个坑：安装了包但 import 失败 🚨

比如你执行：

```bash
python -m pip install requests
```

但是代码：

```python
import requests
```

还是报错。

常见原因是：

> **你安装包的环境，和运行代码的环境不是同一个。**

解决办法：

```bash
where python
where pip
```

Windows 用这个。

macOS / Linux：

```bash
which python
which pip
```

看路径是不是项目 `.venv` 里的。

在 PyCharm 里也要检查：

```text
Settings → Project → Python Interpreter
```

确保选的是：

```text
项目目录/.venv/Scripts/python.exe
```

---

# 32. 国内下载慢怎么办？🚀

如果你在国内下载 PyPI 很慢，可以临时使用镜像源，比如清华、阿里等。

临时安装：

```bash
python -m pip install requests -i https://pypi.tuna.tsinghua.edu.cn/simple
```

但是你要注意：

> 公司项目里不要随便换源，尤其内网、私服、制品库有要求时，要按团队规范来。

如果你们公司配置了内部 PyPI 镜像或私服，一般会统一给出 `index-url`。

---

# 33. pip install 的几种常见形式 📌

## 安装普通包

```bash
python -m pip install requests
```

## 安装指定版本

```bash
python -m pip install requests==2.32.3
```

## 安装版本范围

```bash
python -m pip install "requests>=2.32,<3.0"
```

## 从 requirements 安装

```bash
python -m pip install -r requirements.txt
```

## 升级包

```bash
python -m pip install --upgrade requests
```

## 卸载包

```bash
python -m pip uninstall requests
```

## 查看已安装包

```bash
python -m pip list
```

## 导出依赖

```bash
python -m pip freeze > requirements.txt
```

---

# 34. 包管理里你现在最该掌握的命令 ✅

你现在只要把这些背熟，已经能做很多项目了：

```bash
python -m venv .venv
```

```powershell
.venv\Scripts\Activate.ps1
```

```bash
python -m pip install 包名
```

```bash
python -m pip install 包名==版本号
```

```bash
python -m pip uninstall 包名
```

```bash
python -m pip list
```

```bash
python -m pip show 包名
```

```bash
python -m pip freeze > requirements.txt
```

```bash
python -m pip install -r requirements.txt
```

```bash
deactivate
```

---

# 35. 你可以用 Java/Maven 类比理解 ☕🐍

不是完全一样，但可以类比：

| Java / Maven       | Python                                |
| ------------------ | ------------------------------------- |
| `pom.xml`          | `requirements.txt` / `pyproject.toml` |
| Maven 下载依赖         | pip 下载依赖                              |
| 本地 Maven 仓库        | pip 缓存 / 虚拟环境                         |
| dependency version | 包版本号                                  |
| scope=test         | optional/dev dependencies             |
| 构建插件配置             | `[tool.xxx]`                          |

Python 的生态更灵活，但也更容易乱。
所以新手最重要的习惯是：

> **项目环境隔离 + 依赖文件记录。**

---

# 36. 一句话总结 🎯

Python 包管理，你现在可以先记成这套流程：

```text
建项目
↓
创建 .venv
↓
激活 .venv
↓
pip install 安装包
↓
写代码 import 使用包
↓
pip freeze > requirements.txt 记录依赖
↓
别人 pip install -r requirements.txt 还原环境
```

最核心的三个东西：

```text
.venv               隔离环境
pip                 安装包
requirements.txt    记录依赖
```

稍微现代一点再加：

```text
pyproject.toml      项目配置和依赖声明
uv                  更现代的一体化项目/包管理工具
```

你现在别贪多，先把 **`.venv + pip + requirements.txt`** 用熟，这就是 Python 包管理的基本功 🐍📦✨

[1]: https://packaging.python.org/guides/installing-using-pip-and-virtual-environments/ "Install packages in a virtual environment using pip and venv - Python Packaging User Guide"
[2]: https://pip.pypa.io/ "pip documentation v26.1.1"
[3]: https://packaging.python.org/en/latest/guides/writing-pyproject-toml/ "Writing your pyproject.toml - Python Packaging User Guide"
[4]: https://docs.astral.sh/uv/ "uv"
